import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  imgWrapper: {
    position: 'relative',
    alignItems: 'center',
  },
  teks: {
    marginTop: 6,
    fontFamily: 'Nunito-SemiBold',
    color: '#000',
    fontSize: 12,
  },
});
